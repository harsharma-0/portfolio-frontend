import { DOCUMENT } from '@angular/common';import { Injectable, inject } from '@angular/core';import { Meta,Title } from '@angular/platform-browser';import { environment } from '../config/environment';
@Injectable({providedIn:'root'}) export class SeoService {private title=inject(Title);private meta=inject(Meta);private doc=inject(DOCUMENT);set(title:string,description:string,path=''){this.title.setTitle(`${title} | Harsh Vishwakarma`);[['description',description],['og:title',title],['og:description',description]].forEach(([name,content])=>this.meta.updateTag(name.startsWith('og:')?{property:name,content}:{name,content}));let link=this.doc.querySelector<HTMLLinkElement>('link[rel=canonical]');if(!link){link=this.doc.createElement('link');link.rel='canonical';this.doc.head.appendChild(link)}link.href=`${environment.siteUrl}${path}`}}

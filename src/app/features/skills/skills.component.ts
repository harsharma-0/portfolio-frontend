import { Component,computed,signal } from '@angular/core';import { FormsModule } from '@angular/forms';import { RouterLink } from '@angular/router';import { PortfolioStore } from '../../core/services/portfolio.store';import { Skill } from '../../core/models/portfolio.models';import { SectionHeadingComponent,StateComponent } from '../../shared/components/ui.components';import { SeoService } from '../../core/services/seo.service';
@Component({selector:'app-skills',standalone:true,imports:[FormsModule,RouterLink,SectionHeadingComponent,StateComponent],template:`<section class="section"><div class="container"><app-section-heading eyebrow="Skill explorer" title="Practical knowledge across the stack." description="Browse by discipline, then inspect where each capability has been applied."/><div class="toolbar"><label class="form-field"><span class="sr-only">Search skills</span><input [(ngModel)]="search" placeholder="Search skills…"></label><div class="tabs" role="tablist" aria-label="Skill categories">@for(cat of categories();track cat){<button type="button" role="tab" [attr.aria-selected]="category()===cat" [class.active]="category()===cat" (click)="category.set(cat)">{{cat}}</button>}</div></div><div class="skill-layout">@if(filtered().length){<div class="skill-list" role="listbox" aria-label="Skills">@for(skill of filtered();track skill.name){<button type="button" role="option" [attr.aria-selected]="selected()?.name===skill.name" [class.active]="selected()?.name===skill.name" (click)="selected.set(skill)"><span>{{skill.name}}</span><small>{{skill.proficiency}}</small></button>}</div>@if(selected();as skill){<article class="panel detail"><p class="eyebrow">{{skill.category}}</p><h2>{{skill.name}}</h2><span class="level">{{skill.proficiency}}</span><p>{{skill.description}}</p><h3>Related work</h3><div class="links">@for(slug of skill.related_project_slugs;track slug){<a [routerLink]="['/projects',slug]">{{projectTitle(slug)}} →</a>}@empty{<span class="muted">Applied across engineering practice.</span>}</div></article>}}@else{<app-state title="No skills found" message="Try another category or search term."/>}</div></div></section>`,styles:[`.toolbar{display:grid;gap:1rem}.tabs{display:flex;gap:.5rem;overflow:auto;padding-bottom:.5rem}.tabs button{white-space:nowrap;border:1px solid var(--border);border-radius:99px;background:var(--surface);color:var(--muted);padding:.55rem .8rem}.tabs button.active{background:var(--accent);color:#06110e;border-color:var(--accent)}.skill-layout{display:grid;grid-template-columns:.8fr 1.2fr;gap:1.5rem;margin-top:2rem}.skill-list{display:grid;align-content:start;border:1px solid var(--border);border-radius:var(--radius-lg);overflow:hidden}.skill-list button{display:flex;justify-content:space-between;padding:1rem 1.2rem;background:var(--surface);border:0;border-bottom:1px solid var(--border);color:var(--text);text-align:left}.skill-list button.active{background:var(--surface-strong);box-shadow:inset 3px 0 var(--accent)}.skill-list small{color:var(--muted)}.detail{min-height:390px}.detail h2{font-size:clamp(2rem,5vw,4rem);margin:.8rem 0}.level{display:inline-block;color:var(--accent);border:1px solid var(--accent);border-radius:99px;padding:.3rem .6rem}.detail>p:not(.eyebrow){font-size:1.1rem;color:var(--muted);margin:2rem 0}.links{display:grid;gap:.5rem}.links a{color:var(--accent)}@media(max-width:700px){.skill-layout{grid-template-columns:1fr}}` ]})export class SkillsComponent{search='';category=signal('All');selected=signal<Skill|null>(null);categories=computed(()=>['All',...new Set((this.store.data()?.skills||[]).map(s=>s.category))]);filtered=computed(()=>{const skills=(this.store.data()?.skills||[]).filter(s=>(this.category()==='All'||s.category===this.category())&&(!this.search||`${s.name} ${s.description}`.toLowerCase().includes(this.search.toLowerCase())));if(skills.length&&!skills.some(s=>s.name===this.selected()?.name))queueMicrotask(()=>this.selected.set(skills[0]));return skills});constructor(public store:PortfolioStore,seo:SeoService){seo.set('Skills','Explore Harsh Vishwakarma’s backend, frontend, API architecture and machine-learning skills.','/skills')}projectTitle(slug:string){return this.store.data()?.projects.find(p=>p.slug===slug)?.short_title||slug}}

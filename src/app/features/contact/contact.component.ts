import { Component,signal } from '@angular/core';import { FormBuilder,ReactiveFormsModule,Validators } from '@angular/forms';import { ActivatedRoute } from '@angular/router';import { finalize } from 'rxjs';import { ApiService } from '../../core/services/api.service';import { Budget,ContactRequest,Timeline } from '../../core/models/portfolio.models';import { PortfolioStore } from '../../core/services/portfolio.store';import { SectionHeadingComponent } from '../../shared/components/ui.components';import { SeoService } from '../../core/services/seo.service';
@Component({selector:'app-contact',standalone:true,imports:[ReactiveFormsModule,SectionHeadingComponent],template:`<section class="section"><div class="container contact-grid"><div><app-section-heading eyebrow="Start a conversation" title="Tell me what you’re building—or what needs fixing." description="Share the context, constraints and desired outcome. This form sends an email through the FastAPI backend; it does not create an account or store your inquiry in a database."/><div class="contact-notes"><article><strong>Good fit</strong><p>Python backends, FastAPI APIs, Angular interfaces, integrations, debugging and performance work.</p></article><article><strong>Privacy</strong><p>Your submission is used only to respond to this inquiry. Do not include passwords or sensitive production data.</p></article>@if(emailLink();as email){<article><strong>Direct email fallback</strong><a [href]="email">Open your email client →</a></article>}</div></div><div class="panel">@if(success()){<div class="success-view"><span>✓</span><h2>Inquiry received.</h2><p>Your submission was accepted. No promise of an immediate response—Harsh will review the project details when available.</p><button class="btn secondary" type="button" (click)="newInquiry()">Send another inquiry</button></div>}@else{<form [formGroup]="form" (ngSubmit)="submit()" novalidate><div class="grid-2"><label class="form-field">Name<input formControlName="name" autocomplete="name">@if(invalid('name')){<span class="field-error">Enter 2–80 characters.</span>}</label><label class="form-field">Email<input formControlName="email" type="email" autocomplete="email">@if(invalid('email')){<span class="field-error">Enter a valid email.</span>}</label></div><label class="form-field">Service<select formControlName="service"><option value="">Select a service</option>@for(service of store.data()?.services||[];track service.name){<option [value]="service.name">{{service.name}}</option>}</select>@if(invalid('service')){<span class="field-error">Choose a service.</span>}</label><div class="grid-2"><label class="form-field">Budget range<select formControlName="budget">@for(item of budgets;track item.value){<option [value]="item.value">{{item.label}}</option>}</select></label><label class="form-field">Timeline<select formControlName="timeline">@for(item of timelines;track item.value){<option [value]="item.value">{{item.label}}</option>}</select></label></div><label class="form-field">Subject<input formControlName="subject" maxlength="120">@if(invalid('subject')){<span class="field-error">Enter 5–120 characters without line breaks.</span>}<span class="counter">{{form.controls.subject.value.length}} / 120</span></label><label class="form-field">Project details<textarea formControlName="message" maxlength="3000" rows="8"></textarea>@if(invalid('message')){<span class="field-error">Please provide 20–3000 characters.</span>}<span class="counter">{{form.controls.message.value.length}} / 3000</span></label><label class="form-field">Attachment link <small>(optional, HTTP/HTTPS)</small><input formControlName="attachment_link" type="url" placeholder="https://…">@if(invalid('attachment_link')){<span class="field-error">Use a complete HTTP or HTTPS URL.</span>}</label><input class="honeypot" formControlName="website" tabindex="-1" autocomplete="off" aria-hidden="true"><label class="consent"><input formControlName="consent" type="checkbox"><span>I consent to this information being sent by email so Harsh can respond to my inquiry.</span></label>@if(error()){<div class="error-view" role="alert"><strong>Submission failed</strong><p>{{error()}}</p><span>Your input has been preserved. Review it and retry.</span></div>}<button class="btn submit" [disabled]="form.invalid||sending()">{{sending()?'Sending inquiry…':error()?'Retry submission':'Send project inquiry'}}</button></form>}</div></div></section>`,styleUrl:'contact.component.scss'})
export class ContactComponent{sending=signal(false);success=signal(false);error=signal('');budgets:{value:Budget;label:string}[]=[{value:'under-500',label:'Under $500'},{value:'500-1000',label:'$500–$1,000'},{value:'1000-2500',label:'$1,000–$2,500'},{value:'2500-5000',label:'$2,500–$5,000'},{value:'5000-plus',label:'$5,000+'},{value:'discuss',label:'Let’s discuss'}];timelines:{value:Timeline;label:string}[]=[{value:'urgent',label:'Urgent'},{value:'1-2-weeks',label:'1–2 weeks'},{value:'2-4-weeks',label:'2–4 weeks'},{value:'1-3-months',label:'1–3 months'},{value:'flexible',label:'Flexible'}];form=this.fb.nonNullable.group({name:['',[Validators.required,Validators.minLength(2),Validators.maxLength(80)]],email:['',[Validators.required,Validators.email]],service:['',Validators.required],budget:['discuss' as Budget,Validators.required],timeline:['flexible' as Timeline,Validators.required],subject:['',[Validators.required,Validators.minLength(5),Validators.maxLength(120),Validators.pattern(/^[^\r\n]+$/)]],message:['',[Validators.required,Validators.minLength(20),Validators.maxLength(3000)]],attachment_link:['',[Validators.pattern(/^$|^https?:\/\/.+/i)]],website:[''],consent:[false,Validators.requiredTrue]});constructor(private fb:FormBuilder,private api:ApiService,public store:PortfolioStore,route:ActivatedRoute,seo:SeoService){seo.set('Contact','Discuss a Python, FastAPI, Angular or full-stack development project with Harsh Vishwakarma.','/contact');route.queryParamMap.subscribe(q=>{const service=q.get('service');if(service)this.form.controls.service.setValue(service)})}invalid(name:keyof typeof this.form.controls){const c=this.form.controls[name];return c.invalid&&(c.dirty||c.touched)}submit(){if(this.form.invalid){this.form.markAllAsTouched();return}this.sending.set(true);this.error.set('');const raw=this.form.getRawValue();const body:ContactRequest={...raw,attachment_link:raw.attachment_link||null};this.api.contact(body).pipe(finalize(()=>this.sending.set(false))).subscribe({next:()=>{this.success.set(true);this.form.reset({budget:'discuss',timeline:'flexible',website:'',consent:false})},error:e=>this.error.set(e.message)})}newInquiry(){this.success.set(false);this.error.set('')}emailLink(){const route=this.store.profile()?.links['email'];return route?.startsWith('mailto:')?route:null}}

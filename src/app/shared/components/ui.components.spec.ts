import { TestBed } from '@angular/core/testing';import { LikeButtonComponent } from './ui.components';
describe('LikeButtonComponent',()=>{beforeEach(()=>localStorage.clear());it('toggles accessible local appreciation',()=>{const fixture=TestBed.createComponent(LikeButtonComponent);fixture.componentRef.setInput('slug','test-project');fixture.detectChanges();const button=fixture.nativeElement.querySelector('button') as HTMLButtonElement;button.click();fixture.detectChanges();expect(button.getAttribute('aria-pressed')).toBe('true');expect(button.textContent).toContain('Appreciated')})});

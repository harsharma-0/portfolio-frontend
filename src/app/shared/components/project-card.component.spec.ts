import { TestBed } from '@angular/core/testing';import { provideRouter } from '@angular/router';import { Project } from '../../core/models/portfolio.models';import { ProjectCardComponent } from './ui.components';
describe('ProjectCardComponent links',()=>{it('generates the requested project-detail route',()=>{const fixture=TestBed.configureTestingModule({imports:[ProjectCardComponent],providers:[provideRouter([])]}).createComponent(ProjectCardComponent);fixture.componentRef.setInput('project',{id:1,slug:'aws-cost-calculator',title:'AWS Cost Calculator',short_title:'AWS Cost Calculator',summary:'Summary',description:'Description',category:'Full-Stack Development',featured:true,year:2025,status:'Production Work',technologies:['Python'],responsibilities:[],challenges:[],solutions:[],outcomes:[],metrics:[],gallery:[],links:{},display_order:1} satisfies Project);fixture.detectChanges();expect((fixture.nativeElement.querySelector('a.visual') as HTMLAnchorElement).getAttribute('href')).toBe('/projects/aws-cost-calculator')})});

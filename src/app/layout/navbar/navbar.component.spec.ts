import { TestBed } from '@angular/core/testing';import { provideRouter } from '@angular/router';import { NavbarComponent } from './navbar.component';
describe('Navbar resume link',()=>{it('opens the clean public resume path safely',()=>{const fixture=TestBed.configureTestingModule({imports:[NavbarComponent],providers:[provideRouter([])]}).createComponent(NavbarComponent);fixture.detectChanges();const link=fixture.nativeElement.querySelector('a.resume') as HTMLAnchorElement;expect(link.getAttribute('href')).toBe('/assets/documents/Harsh_Vishwakarma_Resume.pdf');expect(link.getAttribute('target')).toBe('_blank');expect(link.getAttribute('rel')).toContain('noopener')})});
